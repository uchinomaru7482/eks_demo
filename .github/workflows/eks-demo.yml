name: deploy eks demo

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'Please type deploy'
        required: true
        default: ''

jobs:
  build-and-push-image:
    if: ${{inputs.command == 'deploy'}}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: echo
        run: echo "run workflow"
